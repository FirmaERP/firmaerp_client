name: firmaerp
description: This FirmaERP repository centralizes the client application and its associated modules.

homepage: https://firmaerp.softyes.com.br
repository: https://github.com/FirmaERP/firmaerp_client

version: 0.1.0+1
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.8.1
  flutter: ^3.32.6

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
